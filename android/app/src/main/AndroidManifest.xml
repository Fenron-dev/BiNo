<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ── Berechtigungen ──────────────────────────────────────────────── -->

    <!-- Internet: für Cloud-KI-Calls (Phase 5) und Open-Graph-Fetch (Phase 2) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Kamera: für Foto-Capture im CaptureSheet -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Mikrofon: für Audio-Aufnahmen (Long-Press FAB) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Medienzugriff Android 13+ (API 33): Fotos und Audio aus der Galerie -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>

    <!-- Medienzugriff vor Android 13 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <!-- Benachrichtigungen (Android 13+): für Erinnerungen und Wochen-Rückblick (Phase 6) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Biometrie: für App-Lock (Phase 6) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <!-- Standort: optional, nur wenn Geo-Tagging aktiviert (Phase 6) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:label="BiNo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <!-- Standard-Launcher-Intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Share-Intent: Text (z. B. aus Browser, WhatsApp, YouTube) -->
            <!-- Phase 2: receive_sharing_intent verarbeitet diese Intents -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>

            <!-- Share-Intent: URLs -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/uri-list"/>
            </intent-filter>

            <!-- Share-Intent: Bilder -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="image/*"/>
            </intent-filter>

            <!-- Share-Intent: Audio -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="audio/*"/>
            </intent-filter>

        </activity>

        <!-- Flutter-Embedding-Metadaten -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

        <!-- ML Kit: OCR-Modell (Lateinisch) beim App-Install vorherunterladen.
             Ohne diesen Eintrag wird das Modell erst beim ersten Scan geladen,
             was offline fehlschlägt oder mehrere Sekunden Verzögerung erzeugt. -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr"/>

    </application>

    <!-- Intent-Queries für Flutter-Textverarbeitung -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
